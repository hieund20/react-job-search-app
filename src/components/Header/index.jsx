import React from 'react';
import PropTypes from 'prop-types';
import './style.scss';

Header.propTypes = {};

function Header() {

    return (
        <header>
            <span>Github</span>
            <span>Jobs</span>
        </header>
    );
}

export default Header;